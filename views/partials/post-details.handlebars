<section class="card post">
  <a href="/post/{{id}}">
    <header>
      <h4>{{title}}</h4>
      <h6>Posted by {{user.username}} on {{format_date creation_date}}</h6>
    </header>
    <p>{{content}}</p>
  </a>
</section>